/**
 * Toggle billing UI routes. Set to `true` when the real payment gateway is ready.
 * Billing APIs, Redux, hooks, and components remain unchanged either way.
 */
export const ENABLE_BILLING_PAGE = false;
